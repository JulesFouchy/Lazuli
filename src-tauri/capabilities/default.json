{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the Lapis window needs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "core:window:allow-set-theme",
      "comment": "The title bar is drawn by Windows, not by the page, so the only way it follows the app's own light/dark choice is to tell the window about it."
    },
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-confirm",
    "dialog:allow-message",
    {
      "identifier": "opener:allow-open-path",
      "comment": "Opening a project folder in Explorer. A project can live anywhere the user keeps one, so the scope cannot be narrower than the whole filesystem; the only paths passed are ones already in the recents list.",
      "allow": [{ "path": "**" }]
    }
  ]
}
